applications:
- name: rds-broker
  memory: 256M
  disk_quota: 256M
  buildpack: go_buildpack
  command: paas-rds-broker --config ./config.json --port $PORT
env:
  AWS_ACCESS_KEY_ID: "your-aws-access-key-id"
  AWS_SECRET_ACCESS_KEY: "your-aws-secret-access-key"
  GO_INSTALL_PACKAGE_SPEC: "."
